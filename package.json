{
  "name": "r2c-scan",
  "version": "2.0.0",
  "description": "R2C-Scan — Gestão Inteligente de Produtos, Manutenção e Estoque",
  "private": true,
  "type": "module",
  "scripts": {
    "start": "cd backend && node src/server.js",
    "dev": "cd backend && node --watch src/server.js",
    "build": "echo 'Static SPA — no build step required'",
    "lint": "echo 'Lint configured via eslint'",
    "preview": "npx serve . -p 3000",
    "vercel-build": "echo 'Static deployment'"
  },
  "dependencies": {
    "firebase": "^12.13.0",
    "react-hot-toast": "^2.6.0",
    "react-router-dom": "^7.15.1"
  },
  "devDependencies": {
    "@tailwindcss/vite": "^4.3.0",
    "tailwindcss": "^4.3.0"
  }
}